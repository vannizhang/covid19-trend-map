import { ThemeStyle } from "../../AppConfig";

export const HeaderItemHeight = 60;
export const HeaderItemBackgroundColor = '#EFEADC';
export const HeaderItemBorder = `1px solid ${ThemeStyle["theme-color-khaki-dark-semi-transparent"]}`